name: Staging CI

on:
  pull_request:
    branches:
      - main

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  deploy:
    uses: ./.github/workflows/ci.yaml
    with:
      environment: dev
    secrets: inherit
