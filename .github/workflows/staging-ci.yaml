name: Staging CI

on:
  pull_request:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/ci.yml
    with:
      environment: dev
    secrets: inherit
